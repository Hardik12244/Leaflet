<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <%- include('./partials/head') %>
</head>

<style>
    .title-text {
        font-family: 'Playfair Display', serif;
        font-size: 72px;
        font-weight: 700;
        line-height: 1.05;
        letter-spacing: -1.5px;
        color: #242424;
        margin-bottom: 20px;
    }

    .sub-text {
        font-family: 'Source Serif 4', serif;
        font-size: 20px;
        line-height: 1.6;
        color: #6b6b6b;
        max-width: 420px;
        margin-bottom: 30px;
    }


    .main-btn {
        font-family: 'Inter', sans-serif;
        font-size: 18px;
        font-weight: 500;
        padding: 10px 22px;
        border-radius: 999px;
        background: #242424;
        color: #ffffff;
        text-decoration: none;
        transition: 0.2s ease;
    }

    .left-box {
        margin-top: 100px;
    }

    .right-box {
        display: block;
        max-width: 60%;
        margin-right: 0;
    }

    .main-box {
        display: flex;
        margin-left: 80px;
        /* background: #f7f6f2; */
        margin-bottom: 20px;
    }

    .section-strip {
        background: linear-gradient(180deg, #fafafa, #f2f2f2);
        border-top: 1px solid #e6e6e6;
        border-bottom: 1px solid #e6e6e6;
    }

    .section-text {
        font-family: 'Playfair Display', serif;
        font-size: 38px;
        font: bolder;
        font-weight: 600;
        letter-spacing: 0.8px;
        color: #2a2a2a;
        padding-left: 130px;
        padding-top: 40px;
        background: linear-gradient(90deg, #222, #888, #222);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .hero-image {
        max-width: 90%;
        margin-left: 120px;
        height: auto;
    }

    .hero-image {
        -webkit-mask-image:
            linear-gradient(to right, transparent, black 20%, black 80%, transparent),
            linear-gradient(to bottom, transparent, black 20%, black 92%, transparent);

        -webkit-mask-composite: intersect;
        mask-composite: intersect;
    }


    .my-link {
        font-size: 14px;
        color: #242424;
        text-decoration: none;
    }

    .bottom {
        display: flex;
        gap: 40px;
        justify-content: center;
        padding-top: 40px;
        position: static;
    }

    .my-card {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 20px;
        padding: 20px 0;
        text-decoration: none;
        color: inherit;
        border-bottom: 1px solid #e6e6e6;
    }

    .my-card-title {
        font-family: 'Inter', sans-serif;
        font-size: 18px;
        font-weight: 600;
        margin-bottom: 6px;
        color: #242424;
    }

    .my-card-body {
        font-size: 14px;
        color: #6b6b6b;
        margin: 0;
        max-width: 600px;
    }

    .my-card-img {
        width: 200px;
        height: 120px;
        object-fit: cover;
        border-radius: 6px;
        flex-shrink: 0;
    }
</style>

<body>
    <%- include('./partials/script') %>
        <%- include('./partials/nav') %>

            <div class="main-box">

                <div class="left-box">
                    <div class="title-text">Human</div>
                    <div class="title-text">stories & ideas</div>
                    <div class="sub-text">A place to read, write, and deepen your understanding</div>
                    <a href="/user/signup"><button class="main-btn">Start reading</button></a>
                </div>

                <div class="right-box">
                    <img src="/assets/home.jpg" width="800px" class="hero-image">
                </div>

            </div>

            <div class="section-strip">

                <div class="section-text">Latest Blogs</div>

                <div class="container mt-5 ">
                    <div class="row row-cols-4">
                        <% blogs.forEach(blog=> { %>

                            <div class="col-12">
                                <a href="/blog/<%= blog._id %>" class="my-card">

                                    <div class="my-card-content">
                                        <h5 class="my-card-title">
                                            <%= blog.title %>
                                        </h5>
                                        <p class="my-card-body">
                                            <%= blog.body.split(" ").slice(0, 40).join(" ") %>
                                        </p>
                                        <div><%= new Date(blog.createdAt).toLocaleString('en-IN', {hour: '2-digit',minute: '2-digit',hour12: true,day: '2-digit',month: 'short',year: 'numeric'}) %></div>
                                        <span>'Click to read more...'</span>
                                    </div>
                                    <img src=" <%=blog.coverImage %>" class="my-card-img">

                                </a>
                            </div>
                            <% }) %>
                    </div>
                </div>

                <div class="bottom">
                    <a class="my-link" aria-current="page" href="/">Help</a>
                    <a class="my-link" aria-current="page" href="/">About</a>
                    <a class="my-link" aria-current="page" href="/">Privacy</a>
                    <a class="my-link" aria-current="page" href="/">Terms</a>
                </div>
            </div>




</body>

</html>